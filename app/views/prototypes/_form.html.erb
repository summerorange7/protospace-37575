<%= form_with model: prototype, local: true do |f|%>
  <%# binding.pry %>
  <%# 「<%」がつくとrubyの記述を埋め込んでいる%>
  <%# 「<div...」とか「<a...」で始まると通常のHTML%>
  <%# だからbinding.pryを埋める時は「<%」で書き始める（bind..もrubyの記述）%>
  <%# ちなみに、コントローラー内では.newでインスタンスを作ってる
      　　　→ _form内でbinding.pry仕掛けて中身チェックするとどのキーもnilで返ってくる%>
  
  <%#prototype :部分テンプレを使う先のキー名・newアクションのページでは@prototypeを指定
  → newアクションで@prototypeを使うためにキー名で「prototype」として指定して、_form内（ココ）で使う%>
  <div class="field">
    <%= f.label :title, "プロトタイプの名称" %><br />
    <%= f.text_field :title, id:"prototype_title" %>
  </div>

  <div class="field">
    <%= f.label :catch_copy, "キャッチコピー" %><br />
    <%= f.text_area :catch_copy, class: :form__text, id:"prototype_catch_copy" %>
  </div>

  <div class="field">
    <%= f.label :concept, "コンセプト" %><br />
    <%= f.text_area :concept, class: :form__text, id:"prototype_concept" %>
  </div>

  <div class="field">
    <%= f.label :image, "プロトタイプの画像" %><br />
    <%= f.file_field :image, id:"prototype_image" %>
  </div>

  <div class="actions">
    <%= f.submit "保存する", class: :form__btn  %>
  </div>
<% end %>

